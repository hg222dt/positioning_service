<%= link_to "Logga ut!", logout_path %>

<div class='center jumbotron'>
	<h3>Hej user! Här är din api-nyckel</h3>
</div>

<div class='row'>
	<div class='col-md-6 col-md-offset-3'>
		<% if @user.authkey != nil %>
			<%= @user.authkey %>
		<% else !@user.admin_revoked_key %>
			<span>Du har ingen aktuell nyckel. </span>
			<%= link_to "Generera ny!", generate_access_token_path %>
		
		<% end %>

		<% if @user.authkey != nil %>
		<%= link_to "Ta bort nyckel", revoke_key_path %>
		<% end %>

	</div>
</div>